version: 0.1
message_providers:
  - type: service_bus
    name: sb
    connection_string: ""
    subscriptions:
      - name: hello
        topic_name: sorted-test
        handler:
          name: default
          args:
            endpoint: "/test"
      - name: anther-one
        topic_name: sorted-test
        handler:
          name: default
          args: 
            endpoint: "/test"

storage_providers:
  - type: in_memory

handlers:
  - type: http
    name: default
    base_uri: "/my-service"
    port: 8000
    retry_policy: exponential
services:
  sample-app:
    build: .
    ports:
      - "8000:80"
  side-car:
    build: ../
    ports:
      - "5000:80"
    configs:
      - source: messaging_config
        target: /app/config.yaml
        
configs:
  messaging_config:
    file: /messaging_config/config.yaml